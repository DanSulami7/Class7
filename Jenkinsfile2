properties([pipelineTriggers([pollSCM('* * * * *')])])

node("Dan"){
    stage("clone"){
        git branch: 'develop', url: 'https://github.com/DanSulami7/Class7.git'

    }
    stage("execute"){
        bat "dir"
        bat "python disk_resizer.py"
    }
}